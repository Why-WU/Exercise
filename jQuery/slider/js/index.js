$.fn.extend({
  slider: function(obj) {
    var defaultset = {
      time: 1000,
      pic: []
    };
    var nweSet = $.extend({}, defaultset, obj);

    var content = $('<div class = "slider-panner"></div>');
    var imgBar = '<div></div>'
  }
});
